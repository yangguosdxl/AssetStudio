## 1. 命令行参数扩展

- [ ] 1.1 在 `CliOptions` 类中新增 `FileListPath` 属性
- [ ] 1.2 在 `ParseArgs()` 中新增 `-l` / `--list` 参数解析
- [ ] 1.3 更新 `ShowHelp()` 添加 `-l` 参数说明

## 2. 文件列表批量分析逻辑

- [ ] 2.1 在 `Main()` 中新增文件列表模式分支：当 `FileListPath` 非空时读取文件列表
- [ ] 2.2 逐行读取文件列表，跳过空行和 `#` 注释行，检查文件是否存在
- [ ] 2.3 逐个分析并导出 xlsx + json，显示进度信息
- [ ] 2.4 全部完成后，若 `-s` 参数启用则调用 `BatchSummaryExporter` 生成汇总报告

## 3. 测试验证

- [ ] 3.1 使用 `test/高误差文件.txt` 执行批量分析，输出到 `report2`，确认汇总报告生成正确
